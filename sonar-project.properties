sonar.projectKey=pyyupsk_fdu
sonar.organization=pyyupsk

# Locale files are translation data, not logic — duplication across locales is by design.
# Test files have parameterized cases that share structure by necessity.
sonar.cpd.exclusions=src/locale/locales/*.ts,tests/unit/compare/comparisons.test.ts
